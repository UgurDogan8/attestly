{
  "name": "@acknowledge/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types and i18n shared between the Forge backend (src/) and the Custom UI app (static/app) — the only legal way across that boundary (tech design §3)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
